function intro(){
    background(bgImg);
    textAlign(CENTER);
    fill(255);
    textFont("Comic Sans ms");
    textSize(35);
    text("Welcome to the space world, press 'space' to start",width/2,height/2-240);
}