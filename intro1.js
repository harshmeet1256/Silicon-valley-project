function intro1(){
    background(bgImg);
    textAlign(CENTER);
    fill(255);
    textFont("Comic Sans ms")
    textSize(35)
    text("Level1: In this level, you have to make the ghost climb the tower in the space. Don't let the ghost fall down and don't touch the climbers press 's' to start",width/2,height/2-240)
}